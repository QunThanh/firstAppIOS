import { StyleSheet } from 'react-native';

const stylesGlobal = StyleSheet.create({
   color: {
      black: '#000',
      white: '#fff',
      backgroundBlurGrey: '#BCB9B4',
      backgroundGrey: '#F3F0F3',
      backgroundGold: '#EFA537',
   },

   iconTabBar: {
      width: 30,
      height: 30,
   },
   miniSize: {
      fontSize: 200,
   },
});

export default stylesGlobal;
